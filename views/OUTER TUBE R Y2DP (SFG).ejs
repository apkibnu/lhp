<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="shortcut icon" href="/images/logo-astra-otoparts-600x600-modified.png" <!-- MATERIAL ICONS -- />
  <!-- STYLESHEET-->
  <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet"/>
  <link rel="stylesheet" href="/CSS/style.css" />

  <script src="/jquery/jquery.min.js"></script>
  <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
  <link href="/select2/select2.min.css" rel="stylesheet" />
  <script src="/select2/select2.min.js"></script>
  <script src="/socket.io-client-4.5.1/dist/socket.io.min.js"></script><script type = "text/javascript" >
      function preventBack(){window.history.forward();}
       setTimeout("preventBack()", 0);
       window.onunload=function(){null};
     </script>
  <title><%= namapart %></title>
</head>
<body>
  <header>
    <nav class="navbar navbar-expand-sm navbar-dark py-0 m-0" style="background-color: midnightblue">
      <div class="container-fluid">
        <div class="round-container">
          <a href="#" class="back-round">
            <span class="material-symbols-sharp"> arrow_back </span>
          </a>
        </div>
        <div class="container" style="width: max-content; border: transparent; background-color: white; border-radius: 50px">
          <a class="navbar-brand" href="#">
            <img src="/images/nusametal1.png" id="brand-nusametal" height="53" />
          </a>
        </div>
        <!-- Links -->
        <div class="collapse navbar-collapse" id="mynavbar">
          <div class="col-sm-4 col-lg-6 col-xl-7 ps-5">
            <div class="row d-flex flex-column float-end gs-2">
              <div class="col-12 float-end">
                <div id="clock-container" class="float-end">
                  <script>
                    function Time() {
                      // Creating object of the Date class
                      var date = new Date();
                      // Get current hour
                      var hour = date.getHours();
                      // Get current minute
                      var minute = date.getMinutes();
                      // Get current second
                      var second = date.getSeconds();
                      hour = update(hour);
                      minute = update(minute);
                      second = update(second);
                      // Adding time elements to the div
                      document.getElementById('clock-container').innerHTML = hour + ' : ' + minute + ' : ' + second + ' ';
                      // Set Timer to 1 sec (1000 ms)
                      setTimeout(Time, 1000);
                    }
                    // Function to update time elements if they are less than 10
                    // Append 0 before time elements if they are less than 10
                    function update(t) {
                      if (t < 10) {
                        return '0' + t;
                      } else {
                        return t;
                      }
                    }
                    Time();
                  </script>
                </div>
              </div>
              <div class="col-12 float-end">
                <div id="date-container" class="float-end">
                  <script>
                    var date = new Date();
                    var tahun = date.getFullYear();
                    var bulan = date.getMonth();
                    var tanggal = date.getDate();
                    var hari = date.getDay();
                    var jam = date.getHours();
                    var menit = date.getMinutes();
                    var detik = date.getSeconds();
                    switch (hari) {
                      case 0:
                        hari = 'Minggu';
                        break;
                      case 1:
                        hari = 'Senin';
                        break;
                      case 2:
                        hari = 'Selasa';
                        break;
                      case 3:
                        hari = 'Rabu';
                        break;
                      case 4:
                        hari = 'Kamis';
                        break;
                      case 5:
                        hari = "Jum'at";
                        break;
                      case 6:
                        hari = 'Sabtu';
                        break;
                    }
                    switch (bulan) {
                      case 0:
                        bulan = 'Jan';
                        break;
                      case 1:
                        bulan = 'Feb';
                        break;
                      case 2:
                        bulan = 'Mar';
                        break;
                      case 3:
                        bulan = 'Apr';
                        break;
                      case 4:
                        bulan = 'Mei';
                        break;
                      case 5:
                        bulan = 'Jun';
                        break;
                      case 6:
                        bulan = 'Jul';
                        break;
                      case 7:
                        bulan = 'Ags';
                        break;
                      case 8:
                        bulan = 'Sep';
                        break;
                      case 9:
                        bulan = 'Okt';
                        break;
                      case 10:
                        bulan = 'Nov';
                        break;
                      case 11:
                        bulan = 'Des';
                        break;
                    }
                    var tampilTanggal = tanggal + ' ' + bulan + ' ' + tahun;
                    document.getElementById('date-container').innerHTML = tampilTanggal;
                  </script>
                </div>
              </div>
            </div>
          </div>
          <div class="col-sm-11 col-lg-7 col-xl-7 ps-4">
            <div class="row d-flex flex-row g-1" style="margin-top: -2px !important">
              <div class="col-sm-3 col-lg-3 col-xl-2 m-1 d-grid">
                <div class="shift-indicator">
                  <h5>Shift :</h5>
                  <a>
                    <span class="material-symbols-sharp" id="shift-1"> looks_one </span>
                  </a>
                  <a>
                    <span class="material-symbols-sharp" id="shift-2"> looks_two </span>
                  </a>
                  <a>
                    <span class="material-symbols-sharp" id="shift-3"> looks_3 </span>
                  </a>
                </div>
              </div>
              <div class="col-sm-5 col-lg-6 col-xl-6 m-1 ms-3 d-grid">
                <div class="nama-part px-1">
                  <h4><%= nama1 %> </h4>
                  <h5><%= nrp1 %></h5>
                </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </nav>
  </header>

  <main>
      <div class="col-12 d-grid" id="parts-all">
        <div class="row d-flex justify-content-between">
          <div class="col-8 d-grid p-1 px-3">
            <div class="card p-3" style="border: none;">
              <img src="/images/a. Gbr Loc Sheet/Outer Tube R a.png" class="img-fluid mt-5" alt="Responsive image" id="gambar-model-coverL"/>
              <div class="col-12" id="bagian-atas-outertube">
                <div class="row">
                  <div class="col-2 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                    <div class="card border-danger border-3 d-grid" id="card-content">
                      <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="A">
                        <h1>A</h1>
                      </button>
                    </div>
                  </form>
                  </div>
                  <div class="col-2 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-content">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="B">
                          <h1>B</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                  <div class="col-2 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                    <div class="card border-danger border-3 d-grid" id="card-content">
                      <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="C">
                        <h1>C</h1>
                      </button>
                    </div>
                  </form>
                  </div>
                  <div class="col-2 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-content">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="D">
                          <h1>D</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                  <div class="col-2 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-content">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="E">
                          <h1>E</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                  <div class="col-2 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-content">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="F">
                          <h1>F</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div class="col-4 d-grid ">
            <div class="card p-3" style="border: none;">
              <img src="/images/a. Gbr Loc Sheet/Outer Tube R c.png" class="img-fluid mx-5 " alt="Responsive image" id="gambar-rotate"/>
              <div class="col-12" id="bagian-atas-outertube2">
                <div class="row" id="row-atas-panjang">
                  <div class="col-6 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-content">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="G">
                          <h1>G</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                  <div class="col-6 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-content">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="H">
                          <h1>H</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                </div>
              </div>
              <div class="col-12" id="bagian-bawah">
                <div class="row" id="row-bawah-panjang">
                  <div class="col-6 px-0 d-grid" id="button-bawah">
                    <form class="d-grid p-0" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-contentbawah">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="I">
                          <h1>I</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                  <div class="col-6 px-0 d-grid" id="button-bawah">
                    <form class="d-grid p-0" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-contentbawah">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="J">
                          <h1>J</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                </div>
              </div>
            </div>
          </div>

      <div class="col-12 d-grid" id="parts-all">
        <div class="row d-flex justify-content-between">
          <div class="col-8 d-grid mt-5 px-3">
            <div class="card p-3" style="border: none;">
              <img src="/images/a. Gbr Loc Sheet/Outer Tube R b.png" class="img-fluid mt-5" alt="Responsive image" id="gambar-model-coverL"/>
              <div class="col-12" id="bagian-atas-outertube">
                <div class="row">
                  <div class="col-2 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                    <div class="card border-danger border-3 d-grid" id="card-content">
                      <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="K">
                        <h1>K</h1>
                      </button>
                    </div>
                  </form>
                  </div>
                  <div class="col-2 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-content">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="L">
                          <h1>L</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                  <div class="col-2 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                    <div class="card border-danger border-3 d-grid" id="card-content">
                      <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="M">
                        <h1>M</h1>
                      </button>
                    </div>
                  </form>
                  </div>
                  <div class="col-2 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-content">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="N">
                          <h1>N</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                  <div class="col-2 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-content">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="O">
                          <h1>O</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                  <div class="col-2 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-content">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="P">
                          <h1>P</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div class="col-4 d-grid">
            <div class="card p-3" style="border: none; background-color: transparent;">
              <img src="/images/a. Gbr Loc Sheet/Outer Tube R d.png" class="img-fluid mx-5" alt="Responsive image" id="gambar-rotate"/>
              <div class="col-12" id="bagian-atas-outertube2">
                <div class="row" id="row-atas-panjang">
                  <div class="col-6 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-content">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="Q">
                          <h1>Q</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                  <div class="col-6 px-0 d-grid" id="button-outertube">
                    <form class="d-grid" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-content">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="R">
                          <h1>R</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                </div>
              </div>
              <div class="col-12" id="bagian-bawah">
                <div class="row" id="row-bawah-panjang">
                  <div class="col-6 px-0 d-grid" id="button-bawah">
                    <form class="d-grid p-0" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-contentbawah">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="S">
                          <h1>S</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                  <div class="col-6 px-0 d-grid" id="button-bawah">
                    <form class="d-grid p-0" method="post" action="/<%= namapart %>/<%= idprod %>/<%= line %>/locsheet/<%= reject %> /<%= kode %>/<%= undo %>">
                      <div class="card border-danger border-3 d-grid" id="card-contentbawah">
                        <button type="submit" class="btn d-grid" id="btn-bagianparts" name="kode" value="T">
                          <h1>T</h1>
                        </button>
                      </div>
                    </form>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
  </main>
</body>
</html>